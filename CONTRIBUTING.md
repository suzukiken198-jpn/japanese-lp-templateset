# 共同編集への参加

## 変更の提案

1. リポジトリをForkするか、書き込み権限がある場合はブランチを作ります。
2. 対象テンプレートのフォルダを編集します。他のテンプレートは変更しません。
3. `npm run validate` と、変更したテンプレートの `npm ci` / `npm run build` を実行します。
4. PC・スマホで表示と主なボタンを確認し、Pull Requestへ変更点と確認結果を書きます。

新作の追加は [docs/ADDING-A-TEMPLATE.md](docs/ADDING-A-TEMPLATE.md) を参照してください。

## 投稿する内容

- [LICENSE](LICENSE) 第6項に同意し、本プロジェクトでの公開と、有料契約者への商用許諾を認められる権利がある内容だけを投稿してください。投稿者の著作権は保持されます。第三者の著作権表示・ライセンスは保持してください。
- 実際の顧客データ、APIキー、パスワード、`.env`、`.vercel`、`node_modules`を含めないでください。
- フォームがデモの場合は、送信・受付が完了したように表示しないでください。
- 素材の出典・画像の使用条件・未接続機能を利用ガイドへ明記してください。
- 本プロジェクトの独自ライセンスと第三者のライセンスを混同せず、React等の元の条件はそのまま保持します。

誰でも閲覧できることと、誰でもmainへ直接書き込めることは別です。公開リポジトリではForkとPull Requestで参加でき、直接の書き込みは管理者が招待した共同制作者に限られます。

初期設定ではブランチ保護やレビュー必須ルールは設定していません。GitHub Actionsによる検証はPull RequestとmainへのPushで実行します。
